{
  "name": "nuxt-app",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "docker:dev": "docker compose -f docker-compose.dev.yml up",
    "docker:dev:build": "docker compose -f docker-compose.dev.yml down -v && docker compose -f docker-compose.dev.yml build"
  },
  "dependencies": {
    "@nuxt/fonts": "^0.11.4",
    "@nuxt/test-utils": "^3.19.2",
    "@tailwindcss/vite": "^4.1.12",
    "nuxt": "^4.0.3",
    "tailwindcss": "^4.1.12",
    "vue": "^3.5.20",
    "vue-router": "^4.5.1"
  }
}
